[pytest]
testpaths = src
python_files = test_*.py

# Make src/ the root for imports like "from cart.client.api_client import ApiClient"
pythonpath = src

markers =
    contract: tests that validate external merchant API contracts and breaking changes
    negative: tests that validate graceful degradation and error handling
